# Server-side Google Places API key. Never expose this value in EJS or public JavaScript.
PLACES_API_KEY=your_key_here

# Anthropic API key for Claude Smart Match ranking. Get one at https://console.anthropic.com
# When absent, Smart Match uses the built-in rule-based fallback — the demo works without it.
ANTHROPIC_API_KEY=your_key_here
