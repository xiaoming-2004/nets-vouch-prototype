<%- include('partials/start', { title: 'Vouched' }) %>
<section class="receipt"><div class="success-mark" aria-hidden="true">✓</div><h1>Vouched ✓</h1><p class="eyebrow">Payment-Verified</p><h2><%= transaction.merchantName %></h2><p>You paid with NETS at this merchant.</p><p>Your payment amount stays private.</p>
<form method="POST" action="/transactions/<%= transaction.id %>/done"><button class="button" type="submit">Done</button></form></section>
<%- include('partials/end', { active: transaction.source === 'scan' ? 'scan' : 'home' }) %>
